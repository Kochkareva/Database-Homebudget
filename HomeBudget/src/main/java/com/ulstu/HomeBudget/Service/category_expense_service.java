package com.ulstu.HomeBudget.Service;

import com.ulstu.HomeBudget.Controller.category_expense_controller;
import com.ulstu.HomeBudget.Model.category_expense;

import java.util.List;

public class category_expense_service {
   /* private category_expense_controller category_expense_controller = new category_expense_controller();

    public category_expense_service() {
    }

    public category_expense find_category_expense(int id) {
        return category_expense_controller.findById(id);
    }

    public void save_category_expense(category_expense category_expense) {
        category_expense_controller.save(category_expense);
    }

    public void delete_category_expense(category_expense category_expense) {
        category_expense_controller.delete(category_expense);
    }

    public void update_category_expense(category_expense category_expense) {
        category_expense_controller.update(category_expense);
    }

    public List<category_expense> find_All_category_expenses() {
        return category_expense_controller.findAll();
    }

    */
}
